<div class="star-rating">
    @for (int i = 1; i <= 5; i++)
    {
        <span class="@(i <= Rating ? "fa-solid fa-star" : "fa-regular fa-star")"></span>
    }
</div>

@code {
    [Parameter] public int Rating { get; set; } = 5;
}
